#include <stdio.h>
#include <stdlib.h>

int main()  // 1-100 numbers program second edition.
{
    int onehundred = 1;
    while( onehundred < 101) {
        printf("%d\n", onehundred);
        onehundred++;
    }
    return 0;
}
